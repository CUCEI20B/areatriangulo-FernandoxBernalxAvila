#include <iostream>

using namespace std;

int main() {

    int base, altura, x;

    cout << "Ingrese la base: ";
    cin >> base;
    cout << "Ingrese la altura: ";
    cin >> altura;
    x= (base*altura) / 2;

    cout << "Resultado es: " <<x;

    return 0;
}